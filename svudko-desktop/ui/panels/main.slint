import { Theme } from "../theme.slint";
import { VerticalStatusBar } from "../bars/vertical_status_bar.slint"; 

export component MainPanel inherits Rectangle  {
    background: Theme.base-background;

    VerticalStatusBar { }   
}